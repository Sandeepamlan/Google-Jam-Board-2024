## Arcade Hero: Enter the Artifact Registry Python [GSP153]

```
export REPO_NAME=prod-registry

export REGION=

gcloud artifacts repositories create $REPO_NAME --repository-format=python --location=$REGION

```
